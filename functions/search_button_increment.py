from functions import sense

def search_button_increment(ship, robot_position, mouse_positions, alpha):
    # Find shortest path to button  
    x, y = robot_position
    if True:
         sense.sense(robot_position, mouse_positions, alpha)
    return [], (x,y)